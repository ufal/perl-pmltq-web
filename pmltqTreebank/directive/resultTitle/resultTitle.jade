.result-title
  ul.list-inline
    li
      button.btn.btn-default(
        type="button"
        ng-click="result.tree = result.svg.tree().current - 1"
        ng-disabled="result.svg.tree().current <= 1"
        )
        | &laquo;
    li
      span(ng-bind="result.svg.title()")
    li
      button.btn.btn-default(
        type="button"
        ng-click="result.tree = result.svg.tree().current + 1"
        ng-disabled="result.svg.tree().current >= result.svg.tree().total"
        )
        | &raquo;
