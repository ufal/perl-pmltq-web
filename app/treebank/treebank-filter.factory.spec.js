describe('treebanksFilter', function() {

  beforeEach(module('pmltq.treebank'));

  it('should ...', inject(function(treebanksFilter) {

    //expect(treebanks.doSomething()).toEqual('something');

  }));

});
